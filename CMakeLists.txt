# Stub CMakeLists.txt for cloud-bmt-suite sandbox.
# This repo contains CI tooling only (no C++ source).
# All build matrix jobs are mocked — configure and build succeed immediately.
cmake_minimum_required(VERSION 3.21)
project(bmt-mock LANGUAGES NONE)
